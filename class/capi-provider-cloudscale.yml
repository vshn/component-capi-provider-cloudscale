parameters:
  kapitan:
    compile:
      - input_paths:
          - ${_base_directory}/component/app.jsonnet
        input_type: jsonnet
        output_path: .
      - input_paths:
          - ${_base_directory}/component/main.jsonnet
        input_type: jsonnet
        output_path: ${_base_directory}/manifests/capi-provider-cloudscale/
      - input_type: external
        output_path: .
        input_paths:
          - ${_kustomize_wrapper}
        env_vars:
          INPUT_DIR: ${_base_directory}/manifests/capi-provider-cloudscale/
        args:
          - ${_base_directory}/manifests/capi-provider-cloudscale-rendered/
      - input_type: external
        output_path: .
        input_paths:
          - ${_base_directory}/scripts/envsubst-dir
        # map env vars from parameters as needed
        env_vars:
          ENVSUBST_VERSION: v1.4.3
          CLOUDSCALE_API_TOKEN: ${capi_provider_cloudscale:variables:cloudscale_api_token}
        args:
          - ${_base_directory}/manifests/capi-provider-cloudscale-rendered
          - \${compiled_target_dir}/capi-provider-cloudscale/10_kustomize
